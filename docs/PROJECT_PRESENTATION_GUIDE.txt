SnakeBiteRx Project Guide (Beginner + Detailed)

This is detailed guide you can use to study and present this project.

------------------------------------------------------------

One-line idea

SnakeBiteRx is a research prototype that estimates likely venom type from:
* wound photo,
* selected symptoms,
* location (country/state),
* context (time since bite, bite circumstance, age, weight).

It returns a probability distribution, not a medical diagnosis.

------------------------------------------------------------

Safety first (must say this in presentation)

* It is not a medical device.
* It is not FDA/CE approved.
* It can be wrong.
* If confidence is low, it shows unknown.
* In real emergencies, users must follow local emergency care immediately.

------------------------------------------------------------

Main goal of the system

The project tries to answer:

"Given image + symptoms + location + context, what is the probability for each venom pattern class?"

It does not try to identify exact snake species with certainty. Species output is a ranked hint.

------------------------------------------------------------

Output classes (same everywhere)

The 5 output classes are:
1. cytotoxic
2. hemotoxic
3. neurotoxic
4. non_venomous
5. not_snakebite

Important: these same labels are used by image model, symptom model, geo model, and final fusion.

------------------------------------------------------------

Full architecture (simple view)

There are 4 branches:

1. Wound image branch (CNN)
2. Symptom branch (knowledge-base probability matrix)
3. Geography branch (country/state prior from geo data)
4. Context branch (small heuristic prior: time, circumstance, age, weight)

Then all 4 are combined by log-space fusion.

------------------------------------------------------------

What goes IN (inputs)

Required for /predict
* file (image upload)
* country (string)

Optional fields (with defaults)
* state (default empty)
* symptoms as JSON string array (default [])
* time_since_bite_hours (default 3.0)
* bite_circumstance (default "unknown")
* age_years (default 35.0)
* weight_kg (default 60.0)

------------------------------------------------------------

What comes OUT (outputs)

Main outputs from /predict:

* final_probability: 5-class final probability vector
* top_class: raw best class from final vector
* top_confidence: probability value of top_class
* prediction_uncertain: true/false
* display_top_class: class to show in UI (unknown when low confidence)

Also returns branch details:
* wound_probability, symptom_probability, geo_probability
* branch top classes/confidences
* wound_detail (per-backbone info + uncertainty details)
* image_quality (blur check)
* selected_symptoms_ranked
* snake_species_top (ranked hints)
* debug object with fusion internals

------------------------------------------------------------

Wound image model (how + why)

Preferred model file
* models/wound_ensemble.pt

Ensemble members
* EfficientNet-B3
* ResNet50
* DenseNet121

Default ensemble fusion weights
* EfficientNet-B3: 0.58
* ResNet50: 0.26
* DenseNet121: 0.16

Why ensemble?
* Different CNNs make different mistakes.
* Weighted averaging of softmax outputs is more stable than a single model.

Fallback model
* models/wound_mobilenet.pt (single backbone, smaller)

------------------------------------------------------------

Why these models, why not others?

Why EfficientNet-B3 + ResNet50 + DenseNet121?
* They are proven, common CNN families with different feature behaviors.
* Together they reduce single-model bias (lighting, texture, skin tone variation, camera quality).
* They are available in mature PyTorch ecosystems, so loading, testing, and deployment are easier.
* This project uses a practical engineering choice: strong accuracy/stability with manageable complexity.

Why not only one bigger model?
* One model can be overconfident on specific data artifacts.
* A single model failure can directly dominate prediction.
* Ensemble gives error diversity and safer behavior for ambiguous images.

Why not very new architectures (ViT-only, ConvNeXt-only, etc.)?
* They can work, but need retraining, careful benchmarking, and more tuning for this exact dataset.
* This project prioritizes reproducible and stable behavior over chasing newest architecture.
* The current stack already includes uncertainty gates and multimodal fallback, so risk is lower.

Why not object detection/segmentation first?
* Current target is class-level venom pattern estimation, not lesion boundary detection.
* Detection pipelines increase annotation and engineering cost.
* Could be a future extension if you collect reliable wound segmentation labels.

Why symptoms/geo/context are kept (not pure deep learning)?
* Clinical signs may not be visible in one photo.
* Geo and context provide useful prior signals.
* Multimodal design gives graceful fallback when the image branch is uncertain or missing.

------------------------------------------------------------

Wound uncertainty logic

Wound result is marked uncertain (wound_uncertain = true) when:

1. max wound confidence < 0.65, or
2. gap between top1 and top2 < 0.12

So, uncertain means the wound softmax is weak or too flat/ambiguous.

------------------------------------------------------------

Final uncertainty logic

After multimodal fusion:
* if final top confidence < 0.60, then
  - prediction_uncertain = true
  - display_top_class = "unknown"

This is a safety behavior to avoid over-confident labels in UI.

------------------------------------------------------------

Symptom model

Source: data/symptom_data/processed/symptom_dataset.csv

How it works:
* each symptom has weighted links to venom classes,
* selected symptoms are summed,
* normalized to make a probability distribution.

No selected symptoms (or unknown symptom keys) => uniform distribution.

------------------------------------------------------------

Geo model

Sources include:
* data/geo_data/snake_geo_clean.csv
* built assets in models/geo_region_prior.json, models/geo_species_table.json

How it works:
* country/state -> venom prior distribution,
* fallback chain: exact region -> country default -> global default.

Important limitation:
* This is based on occurrence-style geographic data, not direct bite incidence records.

------------------------------------------------------------

Context model (small heuristic prior)

Context lightly nudges probabilities, for example:
* very early hours: slight hemo/cyto bump
* later hours: slight neuro bump
* nocturnal/sleeping/indoor/krait-like context: neuro bump
* child/low-weight: very mild adjustments

It is intentionally weak and not a substitute for exam.

------------------------------------------------------------

Fusion math (core idea)

Fusion is in log space:

w_w*log(p_wound) + w_s*log(p_symptom) + w_g*log(p_geo) + w_c*log(p_context)

Then softmax -> final probabilities.

Why log-space?
* better behavior than naive linear mixing,
* cleaner multiplicative combination of evidences.

------------------------------------------------------------

Fusion weight regimes (very important)

A) No wound model loaded
* wound: 0.38
* symptom: 0.22
* geo: 0.22
* context: 0.18

B) Wound model loaded but uncertain
* wound: 0.28
* symptom: 0.28
* geo: 0.24
* context: 0.20

C) Wound model loaded and confident
* wound: 0.80
* symptom: 0.07
* geo: 0.10
* context: 0.03

Interpretation:
* if image is strong, image dominates;
* if image is weak or missing, symptom/geo/context get more say.

------------------------------------------------------------

"Preference" controls that prevent bad overrides

This is likely what you mean by confidence preferences / preference settings:

1. Symptom floor smoothing (0.10)  
   Prevents one-hot symptom vector from killing all other classes.

2. Geo floor smoothing (0.04)  
   Same idea for over-peaked geo priors.

3. Conflict handling  
   If wound is strong (>= 0.50) and symptom argmax disagrees,
   symptom vector is blended toward uniform (mix = 0.55) to avoid checklist override.

4. Final unknown gate (0.60)  
   Low final confidence -> display unknown.

------------------------------------------------------------

Image quality check

* Uses Laplacian variance sharpness score.
* Very lenient policy.
* Retake recommended only for extreme blur (threshold ~32.0).
* It is a technical blur metric, not clinical wound quality.

------------------------------------------------------------

Species ranking output

snake_species_top is generated by combining:
* current venom distribution,
* region table,
* optional symptom text boosts.

This is a hint list, not species confirmation.

------------------------------------------------------------

API routes you should know

* GET / -> service info + links
* GET /health -> health + model loaded status
* GET /symptoms -> allowed symptom keys and labels
* GET /geo/regions -> country/state dropdown data
* POST /predict -> full multimodal prediction
* POST /test/wound -> wound branch only
* POST /test/symptoms -> symptom branch only
* POST /test/geo -> geo branch only
* POST /test/wound/backbone -> single backbone testing
* GET /ui/ and /ui/lab.html -> browser test pages

------------------------------------------------------------

Data layout (important folders)

* data/geo_data/snake_geo_clean.csv -> geo occurrences + venom labels
* data/geo_data/snake_wounds_labels.json -> wound labels manifest
* data/wound_data/training_data.csv -> training index
* data/symptom_data/processed/ -> symptom/context CSVs
* models/ -> checkpoints and built JSON/PKL assets

------------------------------------------------------------

Dev workflow commands

* make setup -> install + build assets + flutter pub get
* make assets -> rebuild geo/symptom assets
* make api -> run FastAPI
* make lab -> API + tunnel + open lab UI
* make dev-all -> API + tunnel + Flutter web
* make stop / make kill -> stop running stack

------------------------------------------------------------

If checkpoint is missing (very common question)

If wound checkpoint is absent:
* API still works,
* image branch becomes uniform prior,
* final ranking mostly comes from symptoms + geo + context,
* API returns warning text (fusion_warning).

You can fix it by:
* placing models/wound_ensemble.pt, or
* setting WOUND_ENSEMBLE_URL / WOUND_CHECKPOINT_URL for download on startup.

------------------------------------------------------------

How to explain confidence in simple English

Use this sentence:

"Confidence here means model certainty inside this system, not guaranteed real-world correctness."

Also mention:
* low confidence is intentionally turned into unknown for safer UI behavior.

------------------------------------------------------------

Known limitations (presentation honesty)

* Not clinically validated as a medical device.
* Dataset bias is possible (region, photo quality, class imbalance).
* Geo priors are occurrence-based, not direct patient-incidence data.
* Symptom entry quality depends on user input.
* Species ranking is approximate.

------------------------------------------------------------

Suggested demo flow (live presentation)

1. Show /health (model status + classes).
2. Show /symptoms and /geo/regions (input catalogs).
3. Run /predict with a sample image.
4. Explain branch outputs (wound/symptom/geo) before final output.
5. Highlight prediction_uncertain and display_top_class safety logic.
6. Show snake_species_top as hints only.

------------------------------------------------------------

Viva / interview Q&A (ready to use)

Q1) Why not use only image model?
A: A wound photo may miss systemic signs. Symptoms and context capture clinical clues; geo adds regional priors. Multimodal is more robust than image-only.

Q2) Why log-space fusion?
A: Log-space combines evidence more stably and avoids poor behavior from simple linear averaging when one modality is over-peaked.

Q3) Why give 80% weight to wound when confident?
A: Because a high-quality, confident image model should not be easily overridden by noisy one-hot symptom entries.

Q4) What happens when confidence is low?
A: If final top confidence is below 0.60, UI-facing class becomes unknown to reduce misleading certainty.

Q5) How do you define wound uncertainty?
A: Wound is uncertain if top confidence < 0.65 or top1-top2 margin < 0.12.

Q6) What if model checkpoint is missing in production?
A: System still runs; wound branch becomes uniform prior, and output depends on symptom+geo+context until checkpoint is added.

Q7) Is species output a true identification?
A: No. It is a ranked hint from venom distribution + region + symptom text boost.

Q8) Is geo prior equal to real bite incidence?
A: No. It is occurrence-derived prior, not direct epidemiological incidence data.

Q9) What are the 5 classes?
A: Cytotoxic, hemotoxic, neurotoxic, non_venomous, not_snakebite.

Q10) Is this deployable for hospital decisions today?
A: Not as a certified clinical device. It is educational/research support and should not replace medical judgement.

Q11) Why these exact three backbones and not others?
A: They are reliable and complementary CNN families with stable PyTorch support. The goal is practical robustness and reproducibility on this dataset, not just newest architecture hype.

Q12) Why not use only Vision Transformer?
A: ViT can be strong, but requires dedicated retraining/tuning and careful data-scale matching. For this project phase, ensemble CNNs gave predictable behavior with lower deployment risk.

Q13) Why not just use one giant model and remove symptoms/geo?
A: Snakebite severity patterns are not always visible from one image. Symptoms and region provide important non-image evidence and improve fallback when image confidence is low.

Q14) Why not trust symptom checklist more than image?
A: Checklist input can be noisy or incomplete. The system smooths symptom influence and reduces override risk when image is strongly confident.

------------------------------------------------------------

60-second presentation script (quick)

"SnakeBiteRx is a multimodal research system for venom-type estimation. It combines wound image CNN output, symptom knowledge-base probabilities, geo priors, and patient context. The image branch uses a 3-model ensemble (EfficientNet-B3, ResNet50, DenseNet121) and uncertainty gates. Final fusion is done in log-space with adaptive weights: image dominates when confident, but symptom and geo gain weight when image is uncertain or missing. The API returns full probability vectors, uncertainty flags, branch-level explanations, and species hints. Safety is built in: low final confidence is shown as unknown. This is educational decision-support, not a medical diagnosis tool."

------------------------------------------------------------

File map for technical references

* Backend API: backend/main.py
* Fusion math/weights: ml/fusion.py
* Global thresholds/classes: ml/config.py
* Wound inference + uncertainty: ml/infer.py
* Symptom scoring: ml/symptom_engine.py
* Geo priors/species ranking: ml/geo_regions.py, ml/geo_species.py
* Image blur scoring: ml/image_quality.py
* Design docs: docs/MODELS_AND_SCORES.md, docs/WHY_AND_WEIGHTS.md

------------------------------------------------------------

Final reminder for your presentation

Always end with:

* "This is a research/education prototype."
* "Confidence is model confidence, not guaranteed truth."
* "Emergency care decisions must follow clinical protocols, not this app alone."

------------------------------------------------------------

Extended Question Bank (Comprehensive Viva Prep)

Note: No document can guarantee literally every possible question, but this section covers the most likely questions across technical, ML, product, deployment, and ethics.

Priority Order (What examiners most likely ask first)

Tier 1 - Must Prepare (very high probability)

1) What problem are you solving?
Short answer: Probabilistic venom-pattern estimation from image + symptoms + geo + context, with uncertainty-aware output.

2) Is this a diagnosis tool / medical device?
Short answer: No. It is educational/research decision-support and explicitly not a certified medical diagnosis device.

3) Why these models (EfficientNet-B3, ResNet50, DenseNet121)?
Short answer: Complementary CNNs reduce single-model failure; ensemble is more stable and robust.

4) Why multimodal, not image only?
Short answer: Photo alone misses many systemic signs. Symptoms and context add clinical signal; geo adds prior.

5) How do you handle low confidence?
Short answer: If final top confidence < 0.60, output is displayed as unknown.

6) How do you define wound uncertainty?
Short answer: Wound is uncertain if max confidence < 0.65 OR top1-top2 gap < 0.12.

7) Why log-space fusion?
Short answer: More stable evidence combination than naive linear averaging, especially when one modality is over-peaked.

8) What are the five classes?
Short answer: cytotoxic, hemotoxic, neurotoxic, non_venomous, not_snakebite.

9) Is species output a true species ID?
Short answer: No. It is a ranked hint based on venom probabilities + region + symptoms.

10) Main limitations?
Short answer: Not clinically validated device; data bias risk; geo prior is occurrence-based; symptom input can be noisy.

Tier 2 - Likely Follow-up

11) Why not ViT-only / latest architecture?
12) Why not one giant model?
13) Why symptom override is reduced when wound is confident?
14) What if checkpoint is missing in production?
15) Is confidence calibrated real-world probability?
16) How does fallback chain work for geo (region -> country -> global)?
17) What does make dev-all do on a fresh clone?
18) How do you test branch-by-branch behavior?
19) What is your strongest safety mechanism?
20) How would you improve this next?

Tier 3 - Nice to Prepare (lower probability, still useful)

21) Why not segmentation pipeline first?
22) Why these exact smoothing floors?
23) What fairness checks would you run?
24) How to make this publication-ready?
25) How to make this clinically deployable?

How to Use This Priority List

Step 1: Memorize Tier 1 perfectly.
Step 2: Prepare 1-2 sentence answers for Tier 2.
Step 3: Keep Tier 3 as backup.

------------------------------------------------------------

A) Project Basics

Q15) What exact problem are you solving?
A: Probabilistic venom-pattern estimation from multimodal signals (image + symptoms + geography + context), with uncertainty-aware output.

Q16) Is this a diagnosis engine?
A: No. It is decision-support for education/research and explicitly not a clinical diagnosis tool.

Q17) Who is the expected user?
A: Developers, students, and research evaluators; not unsupervised patient self-diagnosis in emergencies.

Q18) Why include not_snakebite as a class?
A: To reduce forced venom predictions on irrelevant/non-bite images and improve safer abstention behavior.

B) Data Questions

Q19) What are your key data files?
A: data/geo_data/snake_geo_clean.csv, data/wound_data/training_data.csv, data/symptom_data/processed/symptom_dataset.csv, and model assets in models/.

Q20) Are labels balanced across classes?
A: Usually not perfectly. Class imbalance risk exists and is acknowledged as a limitation.

Q21) How do you handle unknown or noisy labels?
A: Unknown labels are not used as supervised wound targets; uncertainty thresholds and unknown output protect the UI.

Q22) Is geo data incidence data?
A: No. It is occurrence-derived prior, not direct patient incidence.

C) Model Choice and Architecture

Q23) Why CNN ensemble and not one backbone?
A: Better robustness via error diversity, reduced single-model overfitting, and safer behavior on hard images.

Q24) Why EfficientNet-B3 weighted highest?
A: It generally transfers strongly for fine-grained image patterns; weights are tuned/embedded for stable ensemble behavior.

Q25) Why still keep a MobileNet fallback?
A: Lighter deployment option and compatibility for constrained environments.

Q26) Why not train end-to-end multimodal transformer?
A: Requires larger synchronized multimodal datasets and more infrastructure; this system favors modularity, interpretability, and practical deployment.

D) Uncertainty and Confidence

Q27) What is model confidence exactly?
A: Probability mass at argmax within model output space; not calibrated real-world correctness probability.

Q28) Why two uncertainty layers (wound and final)?
A: First layer checks image reliability; second layer checks final fused certainty for safer user-facing display.

Q29) Why final threshold at 0.60?
A: Conservative UI gate to avoid overstating certainty; configurable in ml/config.py.

Q30) Why wound uncertain at max<0.65 or top2 gap<0.12?
A: Captures both low-confidence and ambiguity cases where class separation is weak.

E) Fusion Logic

Q31) Why use log fusion instead of weighted average of probabilities?
A: Log fusion behaves like weighted product-of-experts and avoids certain instability patterns from plain linear mixing.

Q32) Why smooth symptom and geo vectors?
A: To prevent near-one-hot distributions from unfairly vetoing other modalities.

Q33) Why reduce symptom override when image is confident?
A: Checklist input can be noisy; strong image evidence should not be flipped by one noisy symptom selection.

Q34) What if modalities disagree strongly?
A: Conflict handling and adaptive weights reduce abrupt flips; uncertainty can still force unknown.

F) API and Engineering

Q35) How does startup work?
A: Loads symptom catalog, geo payloads, species payload, optionally downloads checkpoint from env URL, then loads wound predictor if checkpoint exists.

Q36) What happens if no checkpoint exists?
A: Wound branch becomes uniform; API still responds with warning and uses symptom+geo+context.

Q37) Is there branch-level testing?
A: Yes: /test/wound, /test/geo, /test/symptoms, and /test/wound/backbone.

Q38) How do clients discover valid symptoms and regions?
A: GET /symptoms and GET /geo/regions.

G) Deployment and Ops

Q39) Can I run after clone with one command?
A: Yes, make dev-all now triggers first-run setup when .venv/uvicorn is missing.

Q40) What external dependencies are still needed?
A: python3 always; flutter for web/mobile client; cloudflared or ngrok for tunnel URLs.

Q41) How to keep service reachable remotely?
A: Use named tunnel/service setup (stable URL) and keep host + API process always online.

Q42) How to stop everything quickly?
A: make stop (or make kill) clears known pids and key ports.

H) Quality, Validation, and Metrics

Q43) Do you report clinical sensitivity/specificity?
A: Not as a certified clinical validation claim in this repository state.

Q44) How should this be evaluated properly?
A: Use held-out datasets, per-class confusion matrix, calibration checks, subgroup analysis, and prospective validation plan.

Q45) Is confidence calibrated?
A: Not guaranteed calibrated as clinical probability; it is model confidence from fused distribution.

I) Ethics and Safety

Q46) What is the biggest misuse risk?
A: Treating output as medical diagnosis and delaying emergency care.

Q47) How do you mitigate harm?
A: Clear disclaimers, unknown fallback, uncertainty flags, and explicit emergency warning.

Q48) Can geography introduce bias?
A: Yes. Geo priors can encode sampling bias and must be interpreted cautiously.

J) Future Improvements

Q49) What is your next technical upgrade?
A: Better calibration + richer validation + cleaner region normalization + optional segmentation/quality pipeline.

Q50) What would make this publication-ready?
A: Robust dataset protocol, transparent metrics, uncertainty calibration, subgroup fairness analysis, and external validation.

Q51) What would make it clinically closer?
A: Regulatory pathway, prospective trials, clinician workflow integration, and strict post-market monitoring.

K) Presentation Defense Questions

Q52) If examiner says "Your model is overfitting", what do you reply?
A: We reduce overfitting risk via ensemble diversity, uncertainty thresholds, multimodal fallback, and explicit limitations; full overfitting diagnosis needs train/val/test drift analysis.

Q53) If examiner says "Why trust your output?"
A: We do not ask blind trust. We provide probability + uncertainty + explainable branch outputs and force unknown when confidence is low.

Q54) If examiner says "Why not remove geo because it is biased?"
A: Geo is used as a weak prior, not a hard rule; fusion and uncertainty controls prevent pure geo dominance.

Q55) If examiner says "What is your single strongest safety feature?"
A: The unknown display gate on low fused confidence, plus explicit non-medical disclaimer.

Q56) If examiner asks "What exactly did you engineer, not just train?"
A: End-to-end multimodal backend, uncertainty policy, adaptive fusion, API routes, dev automation scripts, and presentation-ready explainability outputs.

------------------------------------------------------------

How to answer unexpected questions (strategy)

1) Start with scope: "This is a research prototype, not a diagnostic device." 
2) Explain current design decision and tradeoff.
3) State known limitation honestly.
4) Give concrete next improvement.

Template answer:
"Great question. In current scope, we chose X because Y tradeoff. Limitation is Z. Next step is A with metric B for validation."

------------------------------------------------------------

Final check sentence for your viva

"Our design goal is safe probabilistic decision-support with uncertainty awareness, not overconfident diagnosis."

